info:
  name: PUT - Category (Crear)
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:8080/category
  body:
    type: json
    data: |-
      {
        "name": "Categoría de prueba"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
