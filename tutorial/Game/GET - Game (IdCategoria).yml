info:
  name: GET - Game (IdCategoria)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/game?idCategory=1
  params:
    - name: idCategory
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
